spring:
  config:
    import: application-secret.yml

  application:
    name: "youtube-timelapse"

  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/h2/youtube-timelapse;
    username: sa
    password:

  jpa:
    #    show-sql: true
    open-in-view: false
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        highlight_sql: true
        format_sql: true
        default_batch_fetch_size: 200
    defer-datasource-initialization: true

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

management:
  endpoints:
    web:
      exposure:
        include: "*"

youtube-data-api:
  max-fetch-count: 50

collect-scheduler:
  collect-count: 100
  collect-interval: 900000 # 15m
  # collect-interval: 60000 # 15m
  initial-delay: 5000

#logging:
#  level:
#    org.hibernate.SQL: debug
#    org.hibernate.orm.jdbc.bind: trace
